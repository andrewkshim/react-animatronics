// TODO: Implement tests. Made an initial attempt, but the tests were brittle.
// The issue is that I can't write a thorough test without having a DOM node to
// work with.

test('normalizer', () => {
  expect(true).toBeTruthy();
})
